#include<stdio.h>
#include<conio.h>

int  Count(char *);

int main()
{
            char cSrc[20] = "";
            int Count = 0;

            printf("\n Enter a string : ");
            gets(cSrc);

            Count = Count(cSrc);

            printf("\n Vowels Count in Given String is : %d.", V_Count);

            getch();
            return 0;
}

int Count(char *Str)
{
        int  Cnt = 0;

        //

        return Cnt;
}
